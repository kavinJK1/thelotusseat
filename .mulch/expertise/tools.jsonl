{"type":"failure","description":"console.info(\"msg\", {obj}) renders as \"msg {}\" in the Next 16 dev logger — the structured object is silently dropped. This bit us where a webhook paid-order log was the only durable trace of a sale. Separately, Next 16 refuses to start a second dev server on another port.","resolution":"Serialise the object into the message string with JSON.stringify rather than passing it as a second argument. For dev servers, reuse the already-running one.","classification":"tactical","recorded_at":"2026-07-11T18:54:38.224Z","id":"mx-38ca0f"}
{"type":"failure","description":"Both mcp__playwright and mcp__chrome-devtools refused to launch ('Browser is already in use ... use --isolated') because a parallel Claude session held the shared browser profile. npm i playwright then failed too: no cached chromium_headless_shell binary.","resolution":"Install playwright with --no-save into the scratchpad and launch via chromium.launch({ channel: 'chrome' }) to drive the system Chrome instead of downloading a browser. This verified the sticky bar end-to-end (off-screen+inert at top, visible when scrolled, display:none on desktop, absent on /checkout) without touching the locked MCP profiles.","classification":"tactical","recorded_at":"2026-07-12T16:48:50.691Z","id":"mx-787db0"}
